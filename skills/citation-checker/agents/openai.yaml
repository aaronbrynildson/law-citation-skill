interface:
  display_name: "Citation Checker"
  short_description: "Audit legal citations in Word footnotes"
  default_prompt: "Use $citation-checker to audit citations in this Word document and report corrections that need manual review."

policy:
  allow_implicit_invocation: true
